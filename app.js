// === === === Variables === === ===
const navBtn = document.getElementById('navBtn');

// === === === Functions === === ===
const openNav = () => {
    console.log ('work');
};

// === === === Events === === ===
navBtn.addEventListener('click', openNav);